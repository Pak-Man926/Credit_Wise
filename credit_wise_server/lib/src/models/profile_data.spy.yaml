class: ProfileData
table: profile_data
fields:
    userId: int, relation(parent=auth_user)
    age: int
    dependants: int
    monthlyIncome: double
    debtRatio: double
    contributorIncome: double

    #user: Users?, relation(name=auth_user)
